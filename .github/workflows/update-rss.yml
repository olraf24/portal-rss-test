name: Update RSS Feed with Groq

on:
  schedule:
    - cron: '*/10 * * * *'  # Co 10 minut (żeby nie przekroczyć limitów API)
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-rss:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Fetch RSS and enhance with Groq
      env:
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      run: |
        cat > fetch-rss-groq.js << 'EOF'
        const https = require('https');
        const fs = require('fs');
        
        const RSS_URL = 'https://www.tvn24.pl/najnowsze.xml';
        const GROQ_API_KEY = process.env.GROQ_API_KEY;
        
        // Funkcja do wywołania Groq API
        async function enhanceDescriptionWithGroq(title, originalDescription) {
          const prompt = `Masz tekst artykułu z serwisu informacyjnego. Przepisz opis tak, żeby był:
        - Krótszy (max 150 znaków)
        - Bardziej angażujący
        - Zachowywał główne fakty
        - Bez emotikonów
        
        Tytuł: ${title}
        Opis: ${originalDescription}
        
        Przepisany opis:`;
        
          const requestData = JSON.stringify({
            messages: [
              {
                role: "user",
                content: prompt
              }
            ],
            model: "llama-3.1-70b-versatile",
            temperature: 0.3,
            max_tokens: 200
          });
        
          return new Promise((resolve, reject) => {
            const options = {
              hostname: 'api.groq.com',
              path: '/openai/v1/chat/completions',
              method: 'POST',
              headers: {
                'Authorization': `Bearer ${GROQ_API_KEY}`,
                'Content-Type': 'application/json',
                'Content-Length': Buffer.byteLength(requestData)
              },
              timeout: 15000
            };
        
            const req = https.request(options, (res) => {
              let data = '';
              res.on('data', chunk => data += chunk);
              res.on('end', () => {
                try {
                  if (res.statusCode !== 200) {
                    console.log(`Groq API error ${res.statusCode}:`, data);
                    resolve(originalDescription); // Fallback do oryginalnego
                    return;
                  }
                  
                  const response = JSON.parse(data);
                  const enhancedText = response.choices[0].message.content.trim();
                  console.log(`Enhanced: ${originalDescription.substring(0, 50)}... → ${enhancedText.substring(0, 50)}...`);
                  resolve(enhancedText);
                } catch (error) {
                  console.log('JSON parse error:', error);
                  resolve(originalDescription);
                }
              });
            });
        
            req.on('error', (error) => {
              console.log('Groq request error:', error);
              resolve(originalDescription);
            });
        
            req.on('timeout', () => {
              console.log('Groq request timeout');
              req.destroy();
              resolve(originalDescription);
            });
        
            req.setTimeout(15000);
            req.write(requestData);
            req.end();
          });
        }
        
        // Funkcja opóźnienia (rate limiting)
        function delay(ms) {
          return new Promise(resolve => setTimeout(resolve, ms));
        }
        
        // Istniejąca funkcja fetchRSS (bez zmian)
        function fetchRSS() {
          return new Promise((resolve, reject) => {
            const options = {
              hostname: 'tvn24.pl',
              path: '/najnowsze.xml',
              method: 'GET',
              headers: {
                'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
                'Accept': 'application/rss+xml, application/xml, text/xml',
                'Accept-Language': 'pl-PL,pl;q=0.9,en;q=0.8',
                'Cache-Control': 'no-cache'
              },
              timeout: 10000
            };
            
            const req = https.request(options, (res) => {
              if (res.statusCode === 301 || res.statusCode === 302) {
                const location = res.headers.location;
                if (location) {
                  const url = new URL(location);
                  const newOptions = {
                    hostname: url.hostname,
                    path: url.pathname + url.search,
                    method: 'GET',
                    headers: options.headers,
                    timeout: 10000
                  };
                  
                  const redirectReq = https.request(newOptions, (redirectRes) => {
                    if (redirectRes.statusCode !== 200) {
                      reject(new Error(`HTTP ${redirectRes.statusCode}`));
                      return;
                    }
                    
                    let data = '';
                    redirectRes.on('data', chunk => data += chunk);
                    redirectRes.on('end', () => resolve(data));
                  });
                  
                  redirectReq.on('error', reject);
                  redirectReq.setTimeout(10000);
                  redirectReq.end();
                  return;
                }
              }
              
              if (res.statusCode !== 200) {
                reject(new Error(`HTTP ${res.statusCode}`));
                return;
              }
              
              let data = '';
              res.on('data', chunk => data += chunk);
              res.on('end', () => resolve(data));
            });
            
            req.on('error', reject);
            req.on('timeout', () => {
              req.destroy();
              reject(new Error('Request timeout'));
            });
            
            req.setTimeout(10000);
            req.end();
          });
        }
        
        // Istniejąca funkcja parseRSS (bez zmian)
        function parseRSS(xml) {
          const items = [];
          const itemParts = xml.split('<item>');
          
          for (let i = 1; i < itemParts.length; i++) {
            const item = itemParts[i].split('</item>')[0];
            
            let title = '';
            let link = '';
            let pubDate = '';
            let description = '';
            
            const titleMatch = item.match(/<title><!\[CDATA\[(.*?)\]\]><\/title>/);
            if (titleMatch) title = titleMatch[1];
            
            const linkMatch = item.match(/<link>(.*?)<\/link>/);
            if (linkMatch) link = linkMatch[1];
            
            const dateMatch = item.match(/<pubDate>(.*?)<\/pubDate>/);
            if (dateMatch) pubDate = dateMatch[1];
            
            const descMatch = item.match(/<description>\s*<!\[CDATA\[([\s\S]*?)\]\]>\s*<\/description>/);
            if (descMatch) {
              let rawDesc = descMatch[1];
              description = rawDesc.replace(/<[^>]*>/g, '').trim();
              description = description.replace(/\s+/g, ' ').trim();
            }
            
            if (title && link) {
              items.push({
                title: title.trim(),
                link: link.trim(),
                pubDate: pubDate.trim(),
                description: description || 'Brak opisu'
              });
            }
            
            if (items.length >= 10) break; // Ograniczamy do 10 dla API
          }
          
          return items;
        }
        
        async function main() {
          try {
            if (!GROQ_API_KEY) {
              throw new Error('GROQ_API_KEY nie jest ustawiony');
            }
            
            console.log('Pobieranie RSS...');
            const xml = await fetchRSS();
            const articles = parseRSS(xml);
            
            console.log(`Pobrano ${articles.length} artykułów, ulepszanie opisów...`);
            
            // Ulepszanie opisów z Groq (sekwencyjnie z opóźnieniem)
            for (let i = 0; i < articles.length; i++) {
              const article = articles[i];
              
              if (article.description && article.description !== 'Brak opisu') {
                console.log(`Ulepszanie ${i + 1}/${articles.length}: ${article.title.substring(0, 30)}...`);
                
                const enhancedDescription = await enhanceDescriptionWithGroq(
                  article.title,
                  article.description
                );
                
                articles[i].originalDescription = article.description;
                articles[i].description = enhancedDescription;
                
                // Opóźnienie między requestami (rate limiting)
                if (i < articles.length - 1) {
                  await delay(1000); // 1 sekunda między requestami
                }
              }
            }
            
            const data = {
              lastUpdate: new Date().toISOString(),
              enhancedWithGroq: true,
              articles: articles
            };
            
            fs.writeFileSync('news.json', JSON.stringify(data, null, 2));
            console.log(`Zapisano ${articles.length} artykułów z ulepszonymi opisami`);
            
          } catch (error) {
            console.error('Błąd:', error);
            process.exit(1);
          }
        }
        
        main();
        EOF
        
        node fetch-rss-groq.js
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add news.json
        if git diff --staged --quiet; then
          echo "Brak zmian"
        else
          git commit -m "Aktualizacja RSS z Groq enhancement $(date)"
          git push
        fi
